<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Edge Connect</title>

    <!-- Bootstrap core CSS -->
    <link href="{{ url_for('static', filename=assets['bootstrap_css']) }}" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="{{ url_for('static', filename=assets['favicon_ico']) }}" >

  </head>

  <body>

    <div class="container">
      <div class="col-md-4">
      </div>
      <div class="col-md-4">
        <form class="form-signin" action="" method="post">
          <h2 class="form-signin-heading">Enter your name</h2>
          <input id="user_name" type="text" class="form-control" name="username" placeholder="Name">
          <p></p>
          <button class="btn btn-lg btn-primary btn-block" type="submit">Sign in</button>
        </form>
      </div>
      <div class="col-md-4">
      </div>
    </div><!-- /.container -->

    <!-- Bootstrap core JavaScript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

    <script src="{{ url_for('static', filename=assets['jquery_js']) }}"></script>
    <script src="{{ url_for('static', filename=assets['bootstrap_js']) }}"></script>
  </body>
</html>
